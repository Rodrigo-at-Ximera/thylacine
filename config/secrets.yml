# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 757353a601872b246c260e9e053aa7445e0b9fad02160b3a875fb9d1f24fade1b4095c10276ddcb1fa28c25368c0f2a708b96f7f6bdc0d2c9b3013c2043b0894

test:
  secret_key_base: 20a6fb8f1ad14fa4175edf52bb2cc361a6e8e9b9997a7fdf6ac7319d8299e4f580dd3e1ea0f6e261ea9ad4294883f8feb167bdb6d327aaf5fd05fc93e58ba590

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

#production:
  #secret_key_base: [in secrets.yml.enc]

#shared:
#  google_maps_api_key: [in secrets.yml.enc]
